<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="pointitem">
<!--시퀀스-->
<select id="sequence" resultType="int">
select point_item_seq.nextval from dual
</select>

<!--C 등록-->
<insert id="insert">
        insert into point_item(  
            point_item_no, point_item_name, point_item_type, point_item_content, 
            point_item_price, point_item_stock
        )
        values(
            #{pointItemNo}, #{pointItemName}, #{pointItemType}, #{pointItemContent},
            #{pointItemPrice}, #{pointItemStock})
            
  </insert>
  <!--U 업데이트
     발생사유 변동가능
     발행량 변동가능-->
  <update id="update">
  update point_item
  set
  	point_item_name = #{pointItemName},
  	point_item_type = #{pointItemType}
  	point_item_content = #{pointItemContent}
  	point_item_price = #{pointItemPrice}
  	point_item_stock = #{pointItemStock}
  where
    point_item_no= #{pointItemNo}
  </update>
  
  <!--R 조회(목록출력) -->
  <select id="selectList" resultType="PointitemDto">
  select * from point_item
  order by point_item_no desc
  </select>
  
    <!--R 번호 상세조회 번호기준 -->
  <select id="selectOneNumber" resultType="PointHistoryDto">
  select * from point_item
  where point_item_no = #{pointItemNo}
  </select>
 
  <!--D 삭제-->
  <delete id="delete">
  delete from point_item
  where point_item_no = #{pointItemNo}
  </delete>
</mapper>



