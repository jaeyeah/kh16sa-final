<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
 <mapper namespace = "watchlist">

	<!-- 등록 -->
	<insert id="insert" parameterType="WatchlistDto">
		insert into watchlist(
			watchlist_content, watchlist_member, watchlist_type
		)
		values(
			#{watchlistContent},#{watchlistMember},#{watchlistType})
	</insert>
	
	<!-- 삭제 -->
    <delete id="delete">
    	delete watchlist
    	where watchlist_content=#{watchlist_content}
			and watchlist_member=#{watchlist_member}
    </delete>

 	
 </mapper>